# Copie este arquivo para .env e preencha os valores
# NUNCA commite o .env com valores reais

# PostgreSQL (Docker local)
POSTGRES_USER=korp
POSTGRES_PASSWORD=korp_dev_2026
POSTGRES_DB=korp

# Estoque.API
ESTOQUE_CONNECTION_STRING=Host=localhost;Port=5432;Database=korp;Username=korp;Password=korp_dev_2026;Search Path=estoque

# Faturamento.API
FATURAMENTO_CONNECTION_STRING=Host=localhost;Port=5432;Database=korp;Username=korp;Password=korp_dev_2026;Search Path=faturamento
ESTOQUE_SERVICE_URL=http://localhost:5002

# Produção (Supabase)
# ESTOQUE_CONNECTION_STRING=Host=<host>.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=<senha>;SSL Mode=Require;Search Path=estoque
# FATURAMENTO_CONNECTION_STRING=Host=<host>.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=<senha>;SSL Mode=Require;Search Path=faturamento
# ESTOQUE_SERVICE_URL=https://korp-estoque.onrender.com
